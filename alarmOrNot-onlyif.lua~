function F(x)
    local x = 3

    if (x < 4)
    then
        return {get = function() return z end,
                set = function(y) z = y end}
    end

    x = 4
end

o1 = F(1)
print(o1.get())
o1.set(20)
print(o1.get())
